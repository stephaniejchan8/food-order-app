MENU ITEM

!1. In Context component, create an array of objects to store each menu item
2. each menu item should be a list item with a key (to link quantity with cart state) - useReducer, Context
3. create a ref for each input amount
4. read user input value (default 1) and add item to a new cart array on button click

CART HEADER
1. number displayed should represent the sum of items - .size()?
2. CSS grow on counter change

CART MODAL
1. create a modal (use portals)
2. add buttons that allow you to decrease/increase the quantity
3. ?use filter/contain to count number of same menu items in array - display in counter
4. calculate total amount value of cart and update - useEffect?
5. close modal button


############# UI ##################

BUTTON
1. darken background on focus/hover 