import styles from './Menu.module.css';
import Card from '../UI/Card/Card';

const Menu = () => {
  return (
    <Card className={styles.menu}>
      <p>test</p>
    </Card>
  );
};

export default Menu;